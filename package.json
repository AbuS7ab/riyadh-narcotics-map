{
  "name": "narco-compliance",
  "version": "0.9.31",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:staging-auth": "node scripts/run-staging-authorization.mjs",
    "check:syntax": "node scripts/check-js.js",
    "check": "npm run check:syntax && git diff --check",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
